[project]
name = "engineering_team_using_crewai"
version = "0.1.0"
description = "Engineering-Team-Using-CrewAI using crewAI"
authors = [{ name = "Your Name", email = "you@example.com" }]
requires-python = ">=3.10,<3.14"
dependencies = [
    "crewai[anthropic,tools]==1.14.4",
]

[project.scripts]
engineering_team_using_crewai = "engineering_team_using_crewai.main:run"
run_crew = "engineering_team_using_crewai.main:run"
train = "engineering_team_using_crewai.main:train"
replay = "engineering_team_using_crewai.main:replay"
test = "engineering_team_using_crewai.main:test"
run_with_trigger = "engineering_team_using_crewai.main:run_with_trigger"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.crewai]
type = "crew"

[tool.uv.workspace]
members = [
    "sandbox",
]
